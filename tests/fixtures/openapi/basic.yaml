openapi: 3.0.3
info:
  title: Basic API
  version: 1.0.0
servers:
  - url: https://api.example.com
paths:
  /users/{user_id}:
    get:
      operationId: getUser
      summary: Fetch a user by ID.
      parameters:
        - name: user_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK

